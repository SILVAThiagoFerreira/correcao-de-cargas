name: Deploy Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

# Keeps the Actions deployment explicit after Pages mode switch.

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare site
        run: |
          mkdir -p public/input
          cp index.html public/
          cp app.js public/
          cp logic.js public/
          cp styles.css public/
          cp openblast-logo.png public/
          cp input/PP210526_EXEC.csv public/input/
          touch public/.nojekyll

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to Pages
        id: deployment
        uses: actions/deploy-pages@v4
